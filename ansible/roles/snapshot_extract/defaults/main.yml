# =============================================================================
# Snapshot Extract Role - Default Variables
# =============================================================================

# Status file locations
extract_status_file: "{{ mount_point }}/.extract-status"
download_status_file: "{{ download_mount_point }}/.download-status"

# Snapshot file location (on shared download disk)
snapshot_file: "{{ download_mount_point }}/snapshot.tar.zst"

# Extraction target
extract_target_dir: "{{ op_reth_data_dir }}"
